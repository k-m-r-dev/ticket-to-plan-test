//! Rust Axum + SQLite Todo API skeleton matching fixtures/todo-api/SPEC.md.
//! Build when `cargo` is available: `cd apps/todo-api && cargo run`

fn main() {
    eprintln!("todo-api: implement handlers per fixtures/todo-api/SPEC.md (oracle: fixtures/todo-api/oracle)");
    eprintln!("Reference HTTP server for oracle CI without rustc: apps/todo-api-reference/server.py");
}
