Greenfield **Rust/Axum/SQLite** Todo HTTP API at `apps/todo-api`: REST CRUD on `/todos` with optional `completed` filter, shared JSON error envelope, env-driven config, sqlx migrations, in-process integration tests, and no auth. Ambiguous ticket open questions were resolved via documented assumptions in `CLARIFICATIONS.md`. Plan is apply-ready; execution waits for explicit approval per Task Handoff Gate.
