# S03: Integration tests + polish

**Goal:** Add in-process axum/tower integration tests for create/list/filter/get/patch/delete, validation failures, and 404 paths; ensure clippy -D warnings clean.
**Demo:** cargo test && cargo clippy -- -D warnings

## Must-Haves

- Required SPEC tests pass via cargo test; cargo clippy -- -D warnings passes; no unfinished placeholders

## Proof Level

- This slice proves: cargo test; cargo clippy -- -D warnings

## Integration Closure

Tests use shared app builder / Router; temp SQLite or equivalent isolation

## Verification

- Test failures name endpoint and expected status

<tasks>
- [ ] **T01**: Happy-path integration tests _(60m)_
  In-process tower/axum tests: create, list, list filter completed=true/false, get, patch, delete. Use temp DATABASE_URL / isolated DB per test. Assert status codes and key JSON fields.
  - Files: `apps/todo-api/tests/api.rs`, `apps/todo-api/src/app.rs`
  - Verify: cargo test
- [ ] **T02**: Validation + 404 integration tests _(45m)_
  Tests: empty title, overlong title (>200), empty PATCH body, bad completed query, 404 for get/patch/delete missing id. Assert validation_error / not_found codes in error envelope.
  - Files: `apps/todo-api/tests/api.rs`
  - Verify: cargo test
- [ ] **T03**: Clippy + README curl examples _(30m)_
  Fix clippy warnings under -D warnings. Optional short README with curl examples only (no OpenAPI/Docker). Confirm validation commands documented for delivery.
  - Files: `apps/todo-api/README.md`, `apps/todo-api/src/`
  - Verify: cargo test; cargo clippy -- -D warnings
</tasks>

## Files Likely Touched

- apps/todo-api/tests/api.rs
- apps/todo-api/src/app.rs
- apps/todo-api/README.md
- apps/todo-api/src/

## Delivery & Guardrails (embedded)

- **Milestone Commit Cadence** (`skills/ticket-to-plan-gsd/SKILL.md`) — do not commit at slice complete; single commit after M003 verified
- **Task Handoff Gate** (`.gsd/workflow/milestone-workflow.md`) — pause after each task; wait for `do next`
- **Commit Gate** (`.gsd/workflow/milestone-workflow.md`) — commit only after verified scope per cadence
- **Remote Mutation Rule** (`.gsd/workflow/milestone-workflow.md`) — no push/PR without approval
- **Validation Rule** (`.gsd/workflow/milestone-workflow.md`) — `cargo test`; `cargo clippy -- -D warnings`
- **GSD Workflow MCP Execution Rule** (`skills/ticket-to-plan-gsd/SKILL.md`) — use `gsd_*` for progress/state
- **Plan-Doc Embed Rule** (`skills/ticket-to-plan-gsd/SKILL.md`) — execution agents must read these embedded rules
