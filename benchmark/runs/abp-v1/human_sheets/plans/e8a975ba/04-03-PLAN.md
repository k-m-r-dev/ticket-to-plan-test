# S03: Integration tests + polish

**Goal:** Add in-process integration tests for create/list/filter/get/patch/delete, validation, and 404 paths; ensure clippy -D warnings passes.
**Demo:** After this: cargo test && cargo clippy -- -D warnings

## Must-Haves

- cargo test covers required cases; cargo clippy -- -D warnings clean; tests use isolated DB (temp file or in-memory) per test where practical

## Proof Level

- This slice proves: tests

## Integration Closure

Tests exercise the same Router as production binary

## Verification

- None beyond test failure messages

<tasks>
- [ ] **T01**: Integration test harness _(45m)_
  Add in-process axum/tower test harness using app() builder and per-test isolated SQLite (temp file or :memory: with migrate). Shared helpers for JSON requests/responses.
  - Files: `apps/todo-api/tests/api.rs`, `apps/todo-api/src/app.rs`
  - Verify: cargo test --test api (harness compiles; smoke health)
- [ ] **T02**: CRUD + filter + 404 tests _(45m)_
  Integration tests: create, list, list filter completed=true/false, get, patch, delete; 404 for get/patch/delete missing ids. Assert status codes and key JSON fields.
  - Files: `apps/todo-api/tests/api.rs`
  - Verify: cargo test --test api
- [ ] **T03**: Validation tests + clippy polish _(30m)_
  Tests: empty title, overlong title (>200), empty PATCH body, bad completed query → 400 validation_error. Run cargo clippy -- -D warnings and fix warnings. Do not add auth/OpenAPI/Docker as required work.
  - Files: `apps/todo-api/tests/api.rs`, `apps/todo-api/src/lib.rs`
  - Verify: cargo test; cargo clippy -- -D warnings
</tasks>

## Files Likely Touched

- apps/todo-api/tests/api.rs
- apps/todo-api/src/app.rs
- apps/todo-api/src/lib.rs

## Delivery & Guardrails (embedded)

- **Milestone Commit Cadence** (`skills/ticket-to-plan-gsd/SKILL.md`) — do not commit at slice complete; single commit after M004 verified
- **Task Handoff Gate** (`.gsd/workflow/milestone-workflow.md`) — pause after each task; wait for `do next`
- **Commit Gate** (`.gsd/workflow/milestone-workflow.md`) — commit only after verified scope per cadence
- **Remote Mutation Rule** (`.gsd/workflow/milestone-workflow.md`) — no push/PR without approval
- **Validation Rule** (`.gsd/workflow/milestone-workflow.md`) — `cargo test`; `cargo clippy -- -D warnings`
- **GSD Workflow MCP Execution Rule** (`skills/ticket-to-plan-gsd/SKILL.md`) — use `gsd_*` for progress/state
- **Plan-Doc Embed Rule** (`skills/ticket-to-plan-gsd/SKILL.md`) — execution agents must read these embedded rules
