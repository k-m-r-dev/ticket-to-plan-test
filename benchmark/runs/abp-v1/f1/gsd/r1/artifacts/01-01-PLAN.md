# S01: Scaffold + persistence

**Goal:** Cargo Axum project with sqlx SQLite, configurable DATABASE_URL/HOST/PORT, migrations for Todo schema, and GET /health
**Demo:** Migrations create todos table; GET /health returns 200

## Must-Haves

- Server starts, migrations applied, /health returns 200 {status:ok}

## Proof Level

- This slice proves: integration

## Integration Closure

Binary boots against sqlite file with schema present

## Verification

- none beyond health status

<tasks>
- [ ] **T01**: Scaffold Cargo Axum project _(30m)_
  Create apps/todo-api Cargo project with axum, tokio, sqlx (sqlite + runtime + migrate), serde, uuid, chrono. Entry point ready for router composition.
  - Files: `apps/todo-api/Cargo.toml`, `apps/todo-api/src/main.rs`
  - Verify: cd apps/todo-api && cargo check
- [ ] **T02**: Add sqlx todos migration _(45m)_
  Add migration creating todos table: id TEXT PK (UUID), title TEXT NOT NULL, completed INTEGER NOT NULL DEFAULT 0, created_at/updated_at TEXT NOT NULL (RFC3339). Run migrations on startup via sqlx.
  - Files: `apps/todo-api/migrations/001_todos.sql`, `apps/todo-api/src/db.rs`, `apps/todo-api/src/main.rs`
  - Verify: cd apps/todo-api && cargo check
- [ ] **T03**: Config + health endpoint _(30m)_
  Read DATABASE_URL (default sqlite:todos.db), HOST (127.0.0.1), PORT (8080). Bind server; GET /health -> 200 {"status":"ok"}. Compose AppState with pool.
  - Files: `apps/todo-api/src/main.rs`, `apps/todo-api/src/app.rs`, `apps/todo-api/src/config.rs`
  - Verify: GET /health returns 200 {status:ok}
</tasks>

## Files Likely Touched

- apps/todo-api/Cargo.toml
- apps/todo-api/src/main.rs
- apps/todo-api/migrations/001_todos.sql
- apps/todo-api/src/db.rs
- apps/todo-api/src/app.rs
- apps/todo-api/src/config.rs

## Delivery & Guardrails (embedded)

- **Milestone Commit Cadence** (`skills/ticket-to-plan-gsd/SKILL.md`) — do not commit at slice complete; single commit after M001 verified
- **Task Handoff Gate** (`.gsd/workflow/milestone-workflow.md`) — pause after each task; wait for `do next`
- **Commit Gate** (`.gsd/workflow/milestone-workflow.md`) — commit only after verified scope per cadence
- **Remote Mutation Rule** (`.gsd/workflow/milestone-workflow.md`) — no push/PR without approval
- **Validation Rule** (`.gsd/workflow/milestone-workflow.md`) — `cargo test`; `cargo clippy -- -D warnings`
- **GSD Workflow MCP Execution Rule** (`skills/ticket-to-plan-gsd/SKILL.md`) — use `gsd_*` for progress/state
- **Plan-Doc Embed Rule** (`skills/ticket-to-plan-gsd/SKILL.md`) — execution agents must read these embedded rules
