# S01: Scaffold + persistence

**Goal:** Scaffold apps/todo-api crate with Axum+Tokio+sqlx SQLite, config (HOST/PORT/DATABASE_URL), migrations for Todo schema, and GET /health.
**Demo:** After this: Migrations create todos table; GET /health returns 200

## Must-Haves

- Crate builds; migrations run on startup; GET /health returns 200 {"status":"ok"}; default bind 127.0.0.1:8080 and sqlite:todos.db

## Proof Level

- This slice proves: binary

## Integration Closure

Router mounts /health; DB pool opened and migrations applied before serve

## Verification

- Startup logs for bind address and migration apply; no metrics required

<tasks>
- [ ] **T01**: Scaffold crate and dependencies _(30m)_
  Create apps/todo-api Cargo project with axum, tokio, sqlx (sqlite + runtime + migrate), serde/serde_json, uuid, chrono (or time), dotenvy optional. Wire binary entrypoint. Include lib or app-builder seam for later in-process tests. DoD: cargo check succeeds.
  - Files: `apps/todo-api/Cargo.toml`, `apps/todo-api/src/main.rs`, `apps/todo-api/src/lib.rs`
  - Verify: cargo check -p todo-api (or from apps/todo-api)
- [ ] **T02**: Config + DB pool + migration _(45m)_
  Read HOST (default 127.0.0.1), PORT (default 8080), DATABASE_URL (default sqlite:todos.db). Open sqlx SqlitePool; run migrations on startup. Add migration creating todos table: id TEXT PK, title TEXT NOT NULL, completed INTEGER/BOOL NOT NULL DEFAULT 0, created_at TEXT NOT NULL, updated_at TEXT NOT NULL. Fail startup on migrate error.
  - Files: `apps/todo-api/src/config.rs`, `apps/todo-api/src/db.rs`, `apps/todo-api/migrations/001_todos.sql`
  - Verify: cargo check; start briefly against temp DB and confirm migration applied
- [ ] **T03**: Health route + app router skeleton _(30m)_
  Implement GET /health → 200 {"status":"ok"}. Build Axum Router with shared AppState (pool). Bind listener from config. Prefer extractable app() builder for future in-process tests.
  - Files: `apps/todo-api/src/routes/health.rs`, `apps/todo-api/src/app.rs`, `apps/todo-api/src/main.rs`
  - Verify: cargo check; optional in-process request to /health returns 200
</tasks>

## Files Likely Touched

- apps/todo-api/Cargo.toml
- apps/todo-api/src/main.rs
- apps/todo-api/src/lib.rs
- apps/todo-api/src/config.rs
- apps/todo-api/src/db.rs
- apps/todo-api/migrations/001_todos.sql
- apps/todo-api/src/routes/health.rs
- apps/todo-api/src/app.rs

## Delivery & Guardrails (embedded)

- **Milestone Commit Cadence** (`skills/ticket-to-plan-gsd/SKILL.md`) — do not commit at slice complete; single commit after M004 verified
- **Task Handoff Gate** (`.gsd/workflow/milestone-workflow.md`) — pause after each task; wait for `do next`
- **Commit Gate** (`.gsd/workflow/milestone-workflow.md`) — commit only after verified scope per cadence
- **Remote Mutation Rule** (`.gsd/workflow/milestone-workflow.md`) — no push/PR without approval
- **Validation Rule** (`.gsd/workflow/milestone-workflow.md`) — `cargo test`; `cargo clippy -- -D warnings`
- **GSD Workflow MCP Execution Rule** (`skills/ticket-to-plan-gsd/SKILL.md`) — use `gsd_*` for progress/state
- **Plan-Doc Embed Rule** (`skills/ticket-to-plan-gsd/SKILL.md`) — execution agents must read these embedded rules
