# ABP planning matrix status

**Protocol:** abp-v1  
**Scaffolded:** 24 / 24 (`awaiting_operator`)  
**Operator-complete plan_ready:** 0 / 24  

Independent Cursor sessions are required per [`OPERATOR_RUNBOOK.md`](../../docs/benchmark/OPERATOR_RUNBOOK.md).  
Do not treat exploratory v0 runs as substitutes for these slots.

```bash
python3 benchmark/validate_runs.py --protocol abp-v1
```
